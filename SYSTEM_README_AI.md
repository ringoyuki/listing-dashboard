# ════════════════════════════════════════════════════════
# 🚨 全AIへ：作業開始前に必ずこのファイルを最初から最後まで読め
# ════════════════════════════════════════════════════════
#
#  このファイルは「出品管理132」プロジェクトの唯一の真実の情報源です。
#
#  ▶ Gemini（Google Antigravity デスクトップ）が来た場合 → このファイルを読め
#  ▶ Claude（Anthropic）が来た場合 → このファイルを読め
#  ▶ ChatGPT / Codex が来た場合 → このファイルを読め
#
#  どのAIが来ても「このファイルを読んでください」とだけヒロキさんに言われる。
#  それだけでここに全ての情報がある。
#
#  作業が終わったら、このファイルの進捗表（タスク一覧）を必ず更新すること。
#  それが次のAIへのバトンになる。
# ════════════════════════════════════════════════════════

---

## 👤 オーナー情報

- **名前**: ヒロキ様
- **役割**: 最終意思決定者。AIは勝手に動くな。必ず報告・確認してから実装。
- **口癖**:「分からない事はコードを書く前に必ず聞くこと」
- **絶対ルール**: スタッフのことは「スタッフ」と呼ぶ（「外注スタッフ」は禁止）

---

## 🎯 このシステムは何か？

**出品管理132ダッシュボード**
- メルカリShops・ヤフオク・ラクマ・ヤフーフリマに出品した商品を統合管理する
- 値下げ・記号変更・タスク管理を自動計算し、スタッフが「ポチポチするだけ」で作業できるダッシュボード
- スタッフが考える必要がない設計（小学生でもわかるUI）がコンセプト

**本番URL（Chromeで確認）**: https://ringoyuki.github.io/listing-dashboard/

---

## 📁 ファイルの場所（2箇所ある！絶対に混乱するな）

| 場所 | 役割 | 備考 |
|------|------|------|
| `C:\Users\hirok\Desktop\Claude\出品管理132\` | ✏️ 作業フォルダ | ここでコードを修正する |
| `C:\Users\hirok\.gemini\antigravity\playground\empty-universe\` | 🚀 Gitリポジトリ | ここからGitHubへpushする |

**デプロイ手順（修正→公開の流れ）**:
```
1. Desktop\Claude\出品管理132\ でコードを修正・テスト
2. 修正ファイルを playground\empty-universe\ にコピー
3. git -C "C:\Users\hirok\.gemini\antigravity\playground\empty-universe" add ファイル名
4. git -C "..." commit -m "説明"
5. git -C "..." push origin main
6. ヒロキさんが https://ringoyuki.github.io/listing-dashboard/ でChrome確認
```

---

## 📂 フォルダ構成（出品管理132）

```
出品管理132/
│
├── 📄 SYSTEM_README_AI.md       ← ★ このファイル。全AIの基地。絶対に読め
├── 📄 index.html                ← UIのメインファイル。モーダル構造が全部ここ
├── 📄 app.js                    ← CSV読み込み・商品データ管理・エラー検知
├── 📄 sale_manager_v3.js        ← ★ 最新版。タスク生成・ソート・報告コピー
├── 📄 sale_manager_v2.js        ← 旧版（参照用。触るな）
├── 📄 sale_manager_v2.js.bak_20260908  ← バックアップ（触るな）
├── 📄 market_search.js          ← 相場検索機能
├── 📄 config.js                 ← 設定値（SALE_INTERVAL等）
├── 📄 style.css                 ← デザイン（基本触るな）
├── 📄 seed_data.js              ← ダミーデータ（読み込みは停止中）
├── 📄 test.js                   ← テスト用（本番無関係）
├── 📄 index.html.orig           ← バックアップHTML（触るな）
├── 📄 headers.txt               ← メルカリCSVのヘッダー名一覧（参照用）
│
├── 📊 product_data_2026-09-04.csv   ← 最新のメルカリCSV（サンプル用）
├── 📊 product_data_2026-07-29.csv   ← 古いCSV
├── 📋 shuppin_data_2026-09-04_1336.json  ← バックアップJSONデータ
│
├── 📁 00_ドキュメント・履歴/    ← 過去のAIの改修ログテキスト
├── 📁 90_サンプルデータ/        ← 開発・テスト用CSVサンプル
├── 📁 BASE出力/                 ← BASE用の出力データ（触るな）
├── 📁 サンプル商品画像データ/   ← 画像（触るな）
└── 📁 画像一括DL/               ← 画像（触るな）
```

---

## ⚙️ システム設定値（config.js）

| 設定 | 値 | 意味 |
|------|----|------|
| `SALE_INTERVAL` | 10 | 記号変更サイクル（日数） |
| `SALE_HALF_DAYS` | 5 | 中間値下げを行うタイミング（5日目） |
| `SALE_DISC_AMT` | 500 | 1回あたりの値下げ額（円） |
| `HIGH_PRICE_ALERT` | 30000 | 高額商品の基準（3万円以上はオーナー許可必須） |
| `REPORT_OVER_DAYS` | 10 | 最終報告までの猶予日数（※実運用は0日が正しい） |
| `SALE_SYMBOLS` | `['●','■','▲','〇','□']` | 記号の順番（●が最高値、□が最終価格） |

**記号ごとの割引率**:
| 記号 | 割引率 | 意味 |
|------|--------|------|
| ● | 100%（元値） | 通常出品 |
| ■ | 95% | 1回目の値下げ |
| ▲ | 90% | 2回目の値下げ |
| 〇 | 80% | 底値圏（要オーナー確認） |
| □ | 72% | 最終価格（要オーナー確認） |

---

## 💡 記号変更の業務フロー（絶対に覚えろ）

```
出品日
  ↓ 10日後
●→■ 記号変更（スタッフが手動でメルカリ説明文を更新）
  ↓ 5日後（15日目）
  ゲリラセール：いいね3以上なら実施（オーナー許可必須）
  ↓ 10日後（25日目）
■→▲ 記号変更
  ↓ ...繰り返し...
▲→〇 【オーナー許可必須】
〇→□ 【オーナー許可必須】
□ で放置 → 0日でも過ぎたらオーナーへ最終報告（再出品or廃棄判断）
```

**スタッフの仕事の流れ（毎朝）**:
1. メルカリShopsからCSVをダウンロードしてGoogleドライブに保存
2. ダッシュボードにCSVを取り込む
3. タスク一覧を上から順にポチポチ完了させる
4. 「📝 オーナーへの本日の報告をコピー」ボタンでLINE報告文を作成
5. ヒロキさんの返答を待って、GOサインが出たら記号変更を実施
6. 作業終了時にJSONデータをGoogleドライブに保存してエクスポート

**GoogleDriveの保存先**:
- CSV → https://drive.google.com/drive/folders/1D0E0xazH8_QBpcy0OiCoHNGO4C373HrG
- JSON → https://drive.google.com/drive/folders/1JHkLM9rVcMcGPOWTCHLZABur7hxcEN0K

---

## 📊 実装タスク一覧（次のAIへのバトン）

### ✅ 完了済み（2026/09/08 Claude作業）

| # | タスク | 変更ファイル | 備考 |
|---|--------|-------------|------|
| 1 | タスクと報告の完全分離・ボタン名変更 | sale_manager_v3.js | 「今日のタスクをコピー」→「オーナーへの報告をコピー」 |
| 2 | 優先ソートの修正（overdueDays絶対降順） | sale_manager_v3.js | priority:-1のハードコード廃止 |
| 3 | 新規商品の誤報告バグ削除 | sale_manager_v3.js | symbolChangedAt===today を完全削除 |

### 🔲 未着手（上から順番にやること）

#### ④ CSVヘッダーの動的パース化 ← **次のAIはここから着手**
- **対象**: `app.js` の `parseCsv()` 関数（L452付近）
- **問題**: `var COL={ID:0, NAME:62, ...}` と列番号をハードコードしている
- **危険**: メルカリが「いいね数」「閲覧数」「登録日時」を追加したので、列がズレると全崩壊
- **修正方針**: CSV1行目のヘッダー名（例: `hdr.indexOf('商品名')`）で動的に取得する
- **メルカリCSV正式列名（headers.txtに全量あり）**:
  - `商品ID`, `商品名`, `商品説明`, `SKU1_在庫数`, `SKU1_商品管理コード`
  - `販売価格`, `商品ステータス`, `ブランドID`, `配送方法`, `発送元の地域`, `発送までの日数`
  - `商品登録日時`（★新列）, `最終更新日時`, `いいね数`（★新列）, `閲覧数`（★新列）
- **参考スクリプト**: `C:\Users\hirok\.gemini\antigravity\brain\a8e99c6f-e028-401c-b1ef-807ac764d22a\scratch\patch_app_csv.py`
  （内容を確認した上で、安全に適用すること）

#### ⑤ 商品登録日時の表示 + いいね数の自動化
- **対象**: `sale_manager_v3.js` の `smRenderPanel()` 関数
- **登録日時**: 詳細パネル上部に `item.shopsRegDate` を「YYYY年MM月DD日」形式で追加表示
- **いいね数自動化**: `item.likes >= 0` の場合、スタッフの手入力欄を非表示にし、
  3以上→パターンB（ゲリラセールあり）、2以下→パターンA（なし）と自動判定

#### ⑥ 強制ウィザード（朝の確認ポップアップ）
- **対象**: `sale_manager_v3.js` の `openSaleModal()` 関数
- **仕様**: 商品管理画面を開いた時、その日初めてなら2ステップの確認を強制表示
  - ステップ1: CSV更新確認 → NOならCSVインポートモーダル強制オープン
  - ステップ2: JSONバックアップ確認 → NOなら設定パネルのエクスポートボタンに誘導
- **嘘発見器**: `csv_import_date` と `last_export_date` のlocalStorageタイムスタンプで照合
- **社長バイパス**: タイトル「商品管理」を3回クリックでスキップ

#### ⑦ 嘘発見器（⑥の中に含まれる）
- タスク⑥の実装時に同時に実装する

#### ⑧ 翌朝CSV答え合わせ（作業ミス警告）
- **対象**: `app.js` の `runImport()` 関数
- **仕様**: 前日完了マークした記号変更と、今日のCSVの実際の記号を照合
  - ズレがある場合→「⚠️ 作業反映エラー」をダッシュボードに赤バナーで表示

#### ⑨ 誤表現の修正
- **対象**: `sale_manager_v3.js` のシミュレーション部分
- `config.js` の `SALE_HALF_DAYS` のコメント「半値引き」→「中間値下げ（5日目）」
- シミュレーション画面の「5日・10日サイクル」→「5日ごとに値下げと記号変更を繰り返す」

---

## 🔧 コーディングの絶対ルール（AIへ）

### 1. ファイルを修正したら必ずGitHubへプッシュ
ヒロキさんはローカルファイルでなくChromeのURL（GitHub Pages）で確認している。
プッシュを忘れると「直したのに変わらない」クレームになる。

### 2. JSファイルのキャッシュは「ファイル名変更」で突破
- `sale_manager_v3.js` を修正したら `sale_manager_v4.js` にリネームしてプッシュ
- `index.html` の `<script src="sale_manager_v3.js">` も同時に変更する

### 3. Pythonでのファイル書き換えルール
- `re.sub()` は絶対に使わない（UnicodeErrorでファイルが0バイトになる）
- `str.replace()` で完全一致置換のみ使う
- 置換前に `assert '対象文字列' in content` で存在確認する
- 書き換え後に `node -c ファイル名.js` で構文チェック必須

### 4. HTMLを直接ツールで編集しない
- `replace_file_content` ツールでHTMLを編集するとターゲット不一致で構造が壊れやすい
- HTMLはPythonスクリプトの `str.replace()` か、完全書き換えで対応する

### 5. 作業前に必ずバックアップ
```powershell
Copy-Item "対象ファイル.js" "対象ファイル.js.bak_日付" 
```

---

## 📅 ヒロキさんとAIのやり取り履歴（重要な決定事項）

### 2026/08/30〜09/02（Claude）
- ヤフオク画像ツールのAPIキーバグ修正
- ステータスフィルター（出品中/非公開/削除）の追加
- メルカリ+1000円・ヤフーフリマ切り捨て・ヤフオク即決価格の実装
- データエクスポート・インポート機能の追加
- **教訓**: JSファイル修正後はキャッシュバスターを上げること。GitHubへのプッシュ忘れを防ぐこと。

### 2026/09/04（Antigravity/Gemini）
- 在庫0の商品をタスクから除外
- seed_data.jsの読み込みを停止（亡霊データ問題を解消）
- 設定エラー自動検知機能（ブランド未登録・配送設定ミス）の追加
- **教訓**: Pythonのre.subでJSファイルを書き換えたらUnicodeErrorで0バイト化した。str.replace()のみ使うこと。

### 2026/09/05（Claude）
- REPORT_OVER_DAYSを10日から2日に変更
- 「一括コピー」ボタン追加（スタッフのベルトコンベア化）
- **教訓**: Flexboxでflex-direction:columnの縦分割時はmin-height:0が必須。

### 2026/09/06（Antigravity/Gemini）
- ボタン無反応バグ（cbtn変数の削除が原因）を修正
- 「報告完了（タイマーリセット）」ボタンを追加
- **教訓**: JSファイル書き換え後はindex.htmlのキャッシュバスターも必ず更新すること。

### 2026/09/07（Antigravity/Gemini Alpha）
- ヒロキさんから14個の問題提起を受け、大規模改修の仕様書を作成
- コード修正はせず、このREADMEへの仕様追記のみ実施
- メルカリからCSV列追加のお知らせがあった（商品登録日時・いいね数・閲覧数）
- **決定事項**:
  - タスクと報告を完全に分離する
  - ゲリラセールは1日と8日の前後4日に集約する
  - 放置猶予は0日（1日でも過ぎたらアウト）
  - 報告はすべて「事前の許可願い」スタイルにする

### 2026/09/08 AM（Claude）
- sale_manager_v3.js として実装（タスク1〜3完了）
- GitHubへのプッシュ確認（ヒロキさんがChromeで確認する人だと気づくのが遅れた）
- index.htmlのレイアウトが一時崩壊→復元
- このREADMEを大幅整備

### 2026/09/08 PM（←今ここ）
- ヒロキさんから「このファイルを全AIの共通引き継ぎ基地にしてほしい」と指示
- ChatGPT Codexでも使うことが判明（Geminiだけでなく複数のAIが交代で作業する）
- このREADMEを完全書き直し（今読んでいるバージョン）

---

## 🐛 やらかした失敗リスト（絶対に繰り返すな）

| 失敗 | 原因 | 対策 |
|------|------|------|
| ファイルが0バイトになった | Pythonのre.sub+UnicodeError | str.replace()のみ使う |
| 「直したのに変わらない」 | GitHubへのプッシュ忘れ | 修正後は必ずpushまでセット |
| キャッシュが古いまま | ?v=数字だけ更新 | JSはファイル名ごと変更する |
| 新規商品が「●⇒●」と誤報告 | symbolChangedAt===todayのバグ | 事後報告ロジックは全廃済み |
| 55日放置が34日放置より下になる | priority:-1のハードコード | overdueDays降順の1本ルールに変更済み |
| HTMLが壊れた | replace_file_contentツールの誤マッチ | HTMLはPythonスクリプトで修正 |
| 「外注スタッフ」と書いた | ルール無視 | 必ず「スタッフ」に統一 |
| 「半値引き」と書いた | 意味の誤解を招く省略 | 「中間値下げ（5日目）」と書く |

---

## 📝 作業後の更新テンプレート（このファイルに追記せよ）

```markdown
### 【YYYY/MM/DD】AIの名前 - 作業内容
- やったこと: 〇〇を修正
- 変更ファイル: xxx.js（v3→v4にリネーム）
- 確認方法: GitHubへプッシュ済み。ヒロキさんChromeで確認。
- タスク進捗: ④ → ✅ 完了
- 次のAIへ: ⑤から着手してください
```

---

*最終更新: 2026/09/08 17:30 JST - Claude*
*次の担当AI: どのAIでも可。タスク④（app.jsのCSV動的パース）から着手すること。*


---

## 【2026/09/08 17:37】Claude — 価格バッジ追加（割り込みタスク）

### やったこと
ヒロキさんより「検索結果の各プラットフォーム行に適正価格を黄色で表示してほしい」と要望。
最優先として割り込み実装。

### 変更ファイル
- `app.js`：`platPriceBadge()` 関数を追加し、`renderCard()` のplat-rowに価格バッジを注入
- `style.css`：`.plat-price` クラスを追加

### 価格計算ルール（実装済み）
| プラットフォーム | 計算式 | 例（18,800円の場合） |
|----------------|--------|---------------------|
| メルカリShops | 基準価格そのまま | ¥18,800 |
| メルカリ | 基準 + 1,000円 | ¥19,800 |
| ヤフオク | 開始: 基準価格 / 即決: +1,500（1〜2万円帯） | 開始¥18,800 即決¥20,300 |
| ラクマ | 基準価格そのまま | ¥18,800 |
| ヤフーフリマ | 1,000円未満切り捨て | ¥18,000 |

### ヤフオク即決価格のルール
- 〜9,999円帯: +1,000円
- 10,000〜19,999円帯: +1,500円
- 20,000円以上: +2,000円

### GitHubへのプッシュ
commit: `feat: 検索結果に各プラットフォームの適正価格を黄色バッジで表示`
push: ✅ main ブランチに反映済み

### 確認URL
https://ringoyuki.github.io/listing-dashboard/
D1063 で検索 → 各プラットフォーム行に黄色の価格バッジが出ることを確認

### 次の作業
割り込みタスク完了後、元のタスク④（CSVヘッダー動的パース化）に戻ること。

---

## 【2026/09/08 17:44】Claude — 価格ボタンをクリックでコピー可能に変更

### やったこと
価格バッジを「押すとその金額がコピーされるボタン」に変更。
ヤフオクは「開始価格」と「即決価格」を別々のボタンに分けた。

### 変更ファイル
- `app.js`：`platPriceBadge()` の `<span>` を `<button onclick="copyPlatPrice()">` に変更
- `copyPlatPrice()` 関数を追加（押したら数字だけクリップボードへ、1.5秒後に元のテキストに戻る）

### 動作
- 各プラットの価格ボタンをクリック → 数字だけがクリップボードにコピーされる
- ボタンが「✅ コピー済み」に変わり、1.5秒後に元の価格表示に戻る
- ヤフオクは「開始¥18,800」「即決¥20,300」を独立した2つのボタンで表示

### GitHubへのプッシュ
commit: `feat: 価格バッジをクリックコピーボタンに変更（ヤフオク即決も独立ボタン）`
push: ✅ main ブランチに反映済み

---

## 📊 現在の全タスク進捗（2026/09/08 17:45 時点）

| # | タスク | 状態 | 備考 |
|---|--------|------|------|
| 1 | タスクと報告の完全分離・ボタン名変更 | ✅ 完了 | sale_manager_v3.js |
| 2 | 優先ソートの修正（overdueDays絶対降順） | ✅ 完了 | sale_manager_v3.js |
| 3 | 新規商品の誤報告バグ削除 | ✅ 完了 | sale_manager_v3.js |
| 割込 | 検索結果に各プラット適正価格を表示 | ✅ 完了 | app.js |
| 割込 | 価格バッジをクリックでコピー可能に | ✅ 完了 | app.js |
| 4 | CSVヘッダーの動的パース化 | 🔲 **次に着手** | app.js |
| 5 | 商品登録日時の表示・いいね数の自動化 | 🔲 未着手 | sale_manager_v3.js |
| 6 | 強制ウィザード（朝のCSV+JSONバックアップ確認） | 🔲 未着手 | sale_manager_v3.js |
| 7 | 嘘発見器（タイムスタンプ照合） | 🔲 未着手 | タスク6と同時実装 |
| 8 | 翌朝CSV答え合わせ（作業ミス警告） | 🔲 未着手 | app.js |
| 9 | 誤表現修正（半値引き→中間値下げ等） | 🔲 未着手 | sale_manager_v3.js |

**次のAIへ: タスク④（CSVヘッダーの動的パース化）から着手すること。**
参考スクリプト: `.gemini/antigravity/brain/a8e99c6f.../scratch/patch_app_csv.py`


---

## 【2026/09/09 07:08】Claude — セール画面の価格表示を修正

### やったこと
スタッフの「セール時の価格がバラバラ」「ラクマが表示されない」という報告を受けて修正。

### 変更ファイル
- `sale_manager_v3.js`：セールありブロック（`smRenderPanel`内の`hasSale`節）の価格グリッドを全面修正

### 変更内容（セール画面のみ。記号変更画面は変更なし）
| 項目 | 修正前 | 修正後 |
|------|--------|--------|
| メルカリShops | セール価格¥X | ✅ セール価格¥X（同じ） |
| メルカリ（フリマ） | セール価格+1,000円❌ | ✅ セール価格¥X（同額） |
| ラクマ | 表示なし❌ | ✅ セール価格¥X（追加） |
| ヤフーフリマ | 価格あり | 参考表示（操作不要と明示） |
| ヤフオク | 表示なし | 参考表示（最低入札額・即決価格を追加） |
| Shops注記 | 「翌日自動で戻す」 | 「タイムセール or 手動で翌日戻す」に変更 |

### 重要な設計決定（次のAIへ）
- **記号変更画面**: メルカリ=Shops+1,000円のルールは変わらない（変更してはいけない）
- **セール画面のみ**: メルカリ/Shops/ラクマは全て同額
- **ヤフオク/ヤフーフリマ**: 実際にはセールしないが参考として表示する
- **②コメント欄**: メルカリ・ラクマのコメントセール用なので残す（削除しない）

### 未解決（次のAIが対応する課題）
スタッフから「いいね3以上でセールのタスクが来るが、セール予約が事前の場合はどうするか」という質問がある。
現在は「セール設定完了ボタンを押した当日起算でスケジュールがリセットされる」ため、
事前予約の場合に数日ズレが生じる可能性がある。→ タスク⑥の強制ウィザードと一緒に解決予定。

### 次に着手すべきタスク
タスク⑤（商品登録日時の表示・いいね数の自動化）から着手すること。
